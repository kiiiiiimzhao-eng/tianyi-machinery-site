# 2026-08-03 工作日志

## 网站体检与修复（天宜机械独立站 tianyi-site）

### 交付物
- `audit/website-audit-report.html`：完整体检报告（合理性评估 + P0/P1/P2 行动清单）。

### 已修复 / 新增
1. **index.html 尾部损坏**：清理游离脚本片段（原 1046-1052 行），恢复合法 `</body></html>`。
2. **部署配置**：`wrangler.jsonc` 的 `assets.directory` 由不存在的 `"./dist"` 改为 `"."`（根目录即资源目录）。
3. **占位链接清理**：
   - index.html：logo `href="#"`→`"/"`；3 个 Read Article `href="#"`→`blog.html`。
   - blog.html：6 个博客卡 `href="#"`→`blog.html`；失效的 `blog/scraper-conveyor-maintenance.html`→`blog.html`。
   - factory.html / blog.html 的 "Projects" 导航 → `projects.html`。
4. **新增页面**（统一模板 header/footer）：
   - `projects.html`：案例展示 + 行业分类筛选 + 指向产品页的内部链接。
   - `404.html`：品牌化错误页（搜索框 + 返回首页/产品）。
   - 两者已加入 `sitemap.xml`（projects.html）。
5. **图片**：
   - `tools/optimize_images.py`：批量重压 JPG（quality 82、最大边 1600、带备份）；实际仅节省 2%（≈0.68MB，因图已较优化），16 个文件被压缩，原图备份于 `images/_optimized_backup/`（已加 .gitignore）。
   - `tools/webp_migrate.py`：进阶方案（转 WebP + 改写 `<picture>`）；实测 WebP 可省 ~34%/12.5MB，**未执行**（改写 HTML 需复核后再 apply）。
   - 删除 `images/about/about-us.jpg`（0 字节废弃文件）。

### 第二轮修复（用户要求：统一域名 + 表单替代）
- **域名统一**：`tools/fix_domain.py` 一次性替换 16 个文件 / 52 处 → 全站 canonical/OG/Schema/页脚邮箱统一为 `tianyimachine.com`（apex，无 www）。产品页原 `www.tianyi-machinery.com` 已全部修正。
- **301 重定向**：新增根目录 `_redirects`（Cloudflare Pages 规范格式），www.tianyimachine.com→apex；旧域名跨域跳转需在 Cloudflare 控制台 Bulk Redirects 配（需旧域也接入 Cloudflare）。
- **联系表单**：Formspree 注册失败，已改为 **Web3Forms**（免注册、填邮箱即生成 access_key）。`index.html` 表单 action→`https://api.web3forms.com/submit`，加 `access_key`(占位 `YOUR_WEB3FORMS_ACCESS_KEY`)、`subject`、`botcheck` honeypot；`_headers` CSP 同步放行 `api.web3forms.com`(connect-src + form-action)。
- 注意：`kim@tianyimachine.com` 邮箱需确保可收信（建议 Cloudflare Email Routing 转发到常用邮箱）。

### 第三轮调整（用户反馈）
- **邮箱回退**：全站 16 个文件 / 32 处联系邮箱从 `kim@tianyimachine.com` 改回 `kim@tianyi-machinery.com`（用户要求邮箱保留旧域名）。网站 URL 仍统一为 `tianyimachine.com`。
- **Web3Forms key 已填入**：`index.html` 表单 access_key 替换为真实值，表单已可用。
- **部署指引**：GitHub Desktop commit summary=`fix: 统一域名、修复死链、新增页面、配置表单`；Cloudflare Pages 构建=None/输出=/；DNS 已有 CNAME→pages.dev（截图确认）；还需绑自定义域名 + SSL Full Strict。
- **301 方案**：用户已在 Cloudflare 控制台「规则→批量重定向」页面，指导其创建 www→apex 列表 + 托管重定向规则；与 `_redirects` 文件二选一。

### 第四轮：问题复检（用户要求验证）
- 全量复检原始体检问题：P0 5 项（部署配置/projects 死链/博客死链/表单/index 尾部损坏）**已全部修复**；P1 中 canonical 统一、404.html、href="#" 清理**已完成**（仅 audit 报告内文字仍含旧描述，属历史快照）。
- **新发现遗漏**：`blog.html` 第 171 行仍残留死链 `blog/scraper-conveyor-maintenance.html`（`blog/` 目录为空）→ 已修正为 `blog.html`。
- **仍存在**：① `certifications.html` 孤立页（无导航/页脚）；② `blog/` 无真实文章页；③ WebP 未执行（可选）。
- 邮箱 `kim@tianyi-machinery.com` 为有意保留（用户要求），非缺陷。

### 待用户处理（仍需决策/操作）
- **部署**：GitHub Desktop 提交 Push 后 Cloudflare Pages 自动部署；控制台 Build 设 Framework=None、Output=`/`，Custom domains 加 `tianyimachine.com` 与 `www`，并开 Always Use HTTPS + Full SSL。
- **certifications.html**：仍是孤立页（无导航/页脚），建议套用统一模板。
- **博客文章页**：`blog/` 仍为空，卡片暂指向 blog.html；建议后续补全真实文章。
- **WebP 迁移**：`tools/webp_migrate.py` 实测可省 ~34%/12.5MB，未执行，复核后 `--apply`。

### 第五轮：certifications.html 套用统一模板（用户要求执行 A）
- 重写 `certifications.html`：补齐全站统一 `.site-header` 导航（含 Certifications 激活态）、面包屑、`.site-footer`、移动端响应式 + 汉堡菜单脚本。
- 新增 OG 标签 + WebPage JSON-LD；底部加转化 CTA 区块（链接到 index.html#contact）。
- 保留原 6 张认证卡片内容；已确认 certifications.html 在 sitemap.xml 中。
- 博客真实文章（B）用户表示后续补充，暂不处理。

### 第六轮：WebP 迁移执行 + belt-conveyor 坏引用修复（用户"一起做吧"）
- **执行 WebP 迁移**（`tools/webp_migrate.py --apply`，已修复脚本缺的 `--dry-run` 参数）：
  - 生成 **318 个 .webp**，改写 **13 个含 `<img>` 的 HTML** 为 `<picture>`（现代浏览器用 webp，老浏览器回退原 jpg，安全无破损）。CSS 背景图（index.html hero-bg.jpg）保留 jpg 不碰。
  - 实测体积：原 JPG 32.44MB → WebP 21.31MB，**节省 34.3%（11.12MB）**。
- **修复 belt-conveyor 产品主图坏引用**：`products/belt-conveyor.html` 第 201-202 行引用 `../images/products/belt-conveyor.jpg/webp`（直接在 products 下，文件不存在）→ 改为存在的 `../images/products/belt-conveyor/main.jpg/webp`。这是**原站就有的坏路径**，非迁移引入。
- **提交**：`822db15` feat(WebP+certifications) + `938668c` fix(belt-conveyor 引用)。工作区干净，待用户 GitHub Desktop Push。
- **重要环境坑（Git for Windows 中文路径）**：
  - `git ls-files '*.webp'` 计数 301 vs 实际 318，差 17 个"未跟踪"——实际是 **certificates/、products/parts/、products/chain-conveyor/ 下的中文名 webp 孤儿**（不被任何 HTML 引用）+ `_optimized_backup/` 备份（被 .gitignore 正确忽略）。
  - **已逐一核查所有被 `<picture>` 引用的 webp（含带空格路径如 factory/workshop 1/、projects/chlor alkali/）全部 TRACKED**，部署零破图风险。
  - 中文名 webp 因 Git for Windows 中文路径 MSYS 转换 bug **加不进 git 且 status 不显示为未跟踪**；但因不被引用、也不部署，对线上无任何影响。可选后续：重命名为英文并引用，或直接删除减小仓库。
- **提交工具提示**：Python 在该环境（MSYS）内嵌调用 `git` 子进程必 segfault；`git ls-files | wc -l` 管道偶发 segfault。改用 `git ls-files > file; wc -l < file` 或纯 bash 循环规避。

### 第七轮：流量为 0 的根因再确认 + www 规范化翻盘（续 traffic-action-plan）
- **状态复检**：git 工作区干净，末提交 `89cb53c` 仅在本地未 Push；线上 `www.tianyimachine.com` 仍是旧版（无 GA4、博客卡仍指向 blog.html）。`nslookup` 实测：**apex `tianyimachine.com` 无任何 A/CNAME 记录（死链）**，`www` 正常解析到 Cloudflare。
- **根因重申**：全站 canonical / sitemap / robots / OG / JSON-LD 全部指向死掉的 apex 域 → Google 抓取的每条 canonical 都解析失败 → 0 收录 → 0 流量。
- **本次修复（本地，未提交）**：
  1. 用 PowerShell（UTF8 无 BOM）把**全部 canonical/og:url/sitemap/robots/JSON-LD/图片/logo** URL 由 `https://tianyimachine.com/...` 改为可用的 `https://www.tianyimachine.com/...`（约 40 个 html/xml/txt，排除 `audit/` 历史文档与 `_redirects`）。
  2. 修正 `_redirects`：**原为 www→apex（apex 又打不开，一旦上线会把所有 www 访客跳进死链）**，已撤销并改为 **apex→www**（裸域若将来补 DNS 则 301 收口到 www；www 自身正常服务）。
  3. 更新 `audit/traffic-action-plan.html`：根因标注「已修复」，步骤 1=Push、步骤 2=可选修 apex DNS；删除「www 会跳到裸域」的错误说法。
- **关键结论（策略翻转）**：改完并 Push 后，网站即可被 Google 经 www 正常抓取收录——**不再依赖 apex DNS 是否修好**。apex DNS 修复降级为「可选：让直接输入裸域的人也能进」的 Nice-to-have。
- **待用户处理**：① GitHub Desktop 提交并 Push（关键，改动均在本地未提交）；② 可选：Cloudflare 给 apex 加自定义域；③ 提供 GA4 衡量 ID（填进 24 页 `GA4_ID`）；④ Push 后由我协助提交 Search Console sitemap。
- **工具记录**：Git Bash 下 Python 处理中文路径必 segfault；批量改造 URL 改用 PowerShell（UTF8Encoding($false)）成功，零 BOM。
