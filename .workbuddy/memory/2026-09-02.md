# 2026-09-02 工作日志

## GEO 第 2、3 项落地
- 选型指南长文（旗舰 GEO 资产）：`blog/bulk-material-handling-equipment-selection-guide.html`（EN）+ `ru/blog/...`（RU）。
  - hero 图用 ImageGen 生成 → 经 `process_blog_image.py` 出 main.jpg(303KB)/main.webp(215KB)。
  - 含 Article + FAQPage(6 条，文字与 schema 逐字一致) JSON-LD、TOC、内链到各产品页、相关阅读、CTA。
  - blog.html(Industry Insights) 与 ru/blog.html(Отраслевые обзоры) 加卡片。
  - sitemap.xml → 28 条；sitemap-ru.xml → 29 条（含 xhtml:link 互链）。
  - 校验全部通过：6/6 FAQ 匹配、hreflang 互链、图片存在。
- 全站 Organization schema：脚本 `inject_org_schema.py` 给 56 个页面（除 index.html 原有）于 `</head>` 前注入 Organization JSON-LD；校验 58 页各 1 个、JSON 合法、0 错误。
- 根目录 `llms.txt`：AI 版 robots.txt，列产品/案例/博客/公司链接 + /ru/ 镜像说明。
- 技能扩充：`.workbuddy/skills/tianyi-geo-product-template/` 加入 inject_org_schema.py，SKILL.md 记录完整 GEO 流程与状态。

## 待办
- 用户 GitHub Desktop 提交并 Push 后，向 GSC/Bing/Yandex 重新提交两个 sitemap，并可在 URL 检查工具验证 FAQ/Organization 富媒体标记。
