# 2026-08-17 工作日志

## Projects 页面项目照片更新
- **用户放入 6 张真实项目照片**到 `images/projects/`（chlor alkali 2 张、mining 4 张），要求更新网站展示。
- **发现并修复多个技术问题**：
  - `chlor alkali/main.JPG` 大写扩展名 vs HTML 引用 `main.jpg` 小写 → Linux 上会 404 裂图。已重命名为小写。
  - 用户替换的 6 张 main.jpg 原始体积巨大（nonferrous metal 17.9MB、biomass 8.7MB、sludge 8.3MB），直接上线会导致页面极慢。已全部缩放至 max 1400px 宽 + WebP 压缩，总大小从 ~40MB+ 降至 ~1.5MB。
  - 6 个文件夹的 `main.webp` 被删导致 `<picture>` source 缺失。已重新生成。
  - 新照片为中文文件名（含空格和中文），不适合直接在 HTML 中引用。已重命名为干净 ASCII 文件名。

### 图片处理结果（Pillow 批量处理）
| 文件夹 | 原始 | 处理后 JPG | WebP |
|---|---|---|---|
| biomass | 5712x4284 / 8.7MB | 218KB | 138KB |
| chlor alkali (main) | 3264x2448 / 2MB | 261KB | 150KB |
| nonferrous metal | 5280x3561 / **17.9MB** | 334KB | 255KB |
| sludge | 6000x3376 / 8.3MB | 141KB | 88KB |
| thermal power | 3968x2976 / 3MB | 149KB | 64KB |
| waste to energy | 1280x557 / 103KB | 115KB | 81KB |

### 新增 5 个真实项目卡片
| 项目 | 文件名 | 分类 |
|---|---|---|
| 云南千万吨级炼油热电联产工程 | refinery-cogen | Power |
| 云南文山800Kta氧化铝煤气工程 | alumina-gas | Chemical |
| 中国铝业炭素厂回转窑煅烧系统 | chalco-kiln | Mining |
| 江西吉安氧化锌粉输送项目 | zinc-oxide | Mining |
| 工业基地航拍全景（微信图片） | plant-aerial | Chemical |

### 最终状态
- projects.html：**13 张卡片**（8 原有 + 5 新增真实项目）
- 所有图片引用均为小写英文文件名，带 WebP 降级 + loading="lazy"
- 待用户 GitHub Desktop Commit + Push 上线

## 2026-08-17 图片 404 修复（第二轮）
- **问题**：第 1 张（Chlor-Alkali）和第 7 张（Urea）卡片图片显示灰色占位符
- **根因**：`main.JPG`（大写）未成功被 git 追踪为重命名，Windows git 默认大小写不敏感
- **修复**：`git rm --cached main.JPG` + `git add main.jpg` 强制暂存重命名，GitHub Desktop 现在可见
- **影响**：两张卡片共用同一张 `chlor alkali/main.jpg`，修一张两张都好
- **Summary**：`fix: 修复 chlor-alkali 项目图片 404（main.JPG 大小写不一致）`

## 2026-08-17 博客页面图片更新
- 用户提供带式输送机长廊实景图，要求替换博客第一张卡片占位符
- 图片保存到 `images/blog/belt-conveyor-installation.jpg`
- blog.html 第 1 张卡片：蓝色渐变+笔图标 → 真实带式输送机图片
- 额外修复：blog.html `main.JPG` → `main.jpg`（大小写 bug）
- **Summary**：`feat: 博客卡片添加带式输送机实景图，修复 main.JPG 大小写引用`

## 2026-08-17 俄语版 factory/certifications 补全 + 提交清单
- 用户确认"两个都做"：补齐 factory.html、certifications.html 的俄语版 + 起草 Yandex/Google 提交清单。
- 新增 RU 页：`ru/factory.html`、`ru/certifications.html`（图片路径改为 `../images/...`，含 hreflang ru/en/x-default、canonical、og:locale ru_RU、RU 导航 + EN 切换）。
- 全站 RU 导航更新：脚本批量将 14 个非首页 RU 页 About Us 下拉的 `/factory.html` 改为 `/ru/factory.html`，并新增 `Сертификаты и награды → /ru/certifications.html`；RU 首页两行链接同步改。
- 英文 `factory.html`、`certifications.html` 加 hreflang 互链 + "RU" 切换（内联样式）。
- 发现并修正：英文站无独立 `contact.html`（联系方式为 index.html#contact 锚点），故 `sitemap-ru.xml` 中 `ru/contact.html` 的英文互链指向 `/#contact`（与 RU 页声明一致），而非不存在的 `/contact.html`。
- `sitemap-ru.xml` 由 15 → 17 条 URL（增 factory、certifications），每条 3 条互链；Python 解析校验通过（17 loc / 51 xhtml:link）。
- 修正 RU 页 logo SVG 笔误（factory 的 class、cert 的 stroke-linecap）。
- 产出：`ru-seo-submission-checklist.md`（Yandex Webmaster + Google Search Console 提交/验证清单，含 GitHub Summary）。
- 待用户 GitHub Desktop Commit + Push，再赴 Yandex/Google 提交 sitemap-ru.xml。

## 2026-08-17 俄语首页"联系我们"导航统一
- **问题**：俄语站子页面（产品/博客等）"Контакты" 指向独立页 `/ru/contact.html`，但俄语**首页** `ru/index.html` 的 4 处链接用 `#contact` 页内锚点（滚动到首页内置联系区块），导致在首页点"联系我们"不跳转到独立俄语联系页，全站行为不一致。
- **修复**：`ru/index.html` 中 4 处 `href="#contact"` → `href="/ru/contact.html"`（导航第505行、Hero按钮525、正文按钮782、页脚1066）。现已全站统一：所有俄语页面"Контакты"/CTA 均跳 `/ru/contact.html`。
- 英文站自洽（首页与子页面均锚点滚动 `index.html#contact`），且用户已决定不建英文 contact 页，故英文侧不动。
- 注：俄语首页仍保留内置 `<section id="contact">` 联系表单区块（仅作为页面内容，不再被导航指向），如需去掉可后续处理。
- 待 GitHub Desktop Commit + Push。
