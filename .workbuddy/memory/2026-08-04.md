# 2026-08-04 工作记录

## 新增 2 篇博客文章（基于现有产品起草）
- `blog/belt-conveyor-installation-guide.html`：皮带机安装与调试指南（含基础准备、逐步安装、空载/负载调试、安全检查、首月维护清单；内嵌产品规格表与产品图，互链 belt-conveyor/parts/valves 页）。
- `blog/industrial-valve-selection-guide.html`：工业阀门选型指南/采购指南（plug-in/flap/slide gate/roller/diverter 对比表，DN150-1000、驱动、密封、压力/温度/防爆，常见选型错误；互链 valves/dust-collector/皮带机文章）。
- 两篇文章均套用全站统一模板（header 导航 + 面包屑 + footer），带 Article JSON-LD、OG 标签、canonical。
- `blog.html`：将"Maintenance Guides"区与"Industry Insights"区各 1 张占位卡改为指向这两篇文章（真实链接，无死链）。
- `sitemap.xml`：新增两篇文章 URL（priority 0.7, weekly/monthly）。
- 已 git commit（7c1fd81），工作区干净。提交后 GitHub Desktop 仅需 Push。

## 补完博客列表剩余 5 张占位卡（用户要求）
- 新增 5 篇真实文章，全部套用统一模板 + Article JSON-LD/OG/canonical：
  1. `blog/belt-conveyor-troubleshooting.html`：5 大常见皮带机故障排查与修复（Maintenance Guide）。
  2. `blog/bucket-elevator-inspection.html`：斗提机日/周/月检查清单 + 防爆安全（Maintenance Guide）。
  3. `blog/uruguay-customer-visit.html`：乌拉圭客户来访工厂（Company News，配 factory/workshop 1 实图）。
  4. `blog/manufacturing-champion-award.html`：获评湖北制造冠军（Company News）。
  5. `blog/chlor-alkali-conveying-system.html`：氯碱行业散料输送系统案例（Industry Insights，配 projects/chlor alkali 实图）。
- `blog.html`（根目录）5 张占位卡 href 改指向上述文章，缩略图改用真实图片（注意：blog.html 在根目录，图片路径用 `images/...` 而非 `../images/...`；带空格路径用 url('...') 引号包裹）。
- `sitemap.xml`：新增 5 篇文章 URL。
- 已分两次 git commit（2313a54 新增文章+sitemap；b2392d4 提交根目录 blog.html）。工作区干净。
- 注意坑：`git add blog/` 只覆盖 blog/ 子目录，根目录 blog.html 需单独 add。

## certifications.html 接入真实证书图（用户要求）
- 原 6 张纯文字卡片（High-Tech/Little Giant/ISO 9001/100+ Patents/Green Factory/Standard Setter）替换为 `images/certificates/` 下 6 张**真实证书图**：
  1. iso9001-quality.webp — ISO 9001 质量管理体系
  2. iso14001-environment.webp — ISO 14001 环境管理体系
  3. iso45001-safety.webp — 职业健康安全管理体系
  4. ce-belt-conveyor.png — 皮带机 CE 认证
  5. little-giant.png — 专精特新小巨人
  6. ip-compliance.webp — 企业知识产权合规管理体系认证
- 原图为中文文件名（如 `2023年天宜质量管理体系证书_英文.webp`），为避免中文 URL 部署隐患与便于管理，用 Python 复制到**英文文件名副本**（非破坏性，原中文文件保留为存档）。
- 卡片改为图片卡：`<a target="_blank">` 点击看大图 + `<img loading="lazy">`；新增 `.cert-intro` 保留 High-Tech/100+专利/Green Factory/标准制定者等荣誉文字。CSS 新增 `.cert-intro/.cert-img-link` 样式。
- 已确认 6 个英文证书图均已被 git 跟踪（此前中文名证书 webp 也在 git 内，部署无碍），commit 978fc35。
- 提示：原中文名证书图未被任何页面引用，仍在仓库中（可后续删除以精简）。

## 备注
- 图片引用均用 `../images/...` 相对路径（与 products 子目录同级），已确认 `images/products/valves/main.webp`、`images/logo-200.png` 存在。
- 用户累计需求已覆盖：技术缺陷清零、WebP、模板统一、表单、域名、博客起步内容、真实证书图。
- 关键决策（2026-08-04）：**tianyimachine.com 与 tianyi-machinery.com 计划独立运营、互不影响**。tianyi-machinery.com 的邮箱(kim@)由现有服务商 mxchina/mxhichina 托管且正常使用 → **结论：不启用、也不应启用 Cloudflare Email Routing**（会与 mxchina 的 MX 冲突、搞挂现有邮箱）；不要删 mxchina 的 MX/mail/pop3/smtp CNAME 与 SPF TXT。新站询盘经 mailto + Web3Forms(access key c6eeec07-...) 发到 kim@tianyi-machinery.com，由旧域邮件服务商收下，新域名无需自建邮件。注意：**不要配置 old→new 批量重定向**（与独立运营矛盾）。旧域 tianyi-machinery.com 在 Cloudflare 扫描出的 apex/www CNAME 指向同一 *.pages.dev，若两域同指一个 Pages 项目则内容重复、非独立；要独立需旧域指向自有源（单独 Pages 项目或原主机）。若仅为 301 而加 Cloudflare、现又不跳转，可把旧域移出 Cloudflare 留在原 DNS 最省心。待确认：旧域未来展示什么（自有旧站 / 同新站 / 仅邮箱域名）。
- 剩余可选项：旧域独立网站部署、孤儿中文 WebP/证书图清理、分析工具(GA4/Plausible)、更多博客。

## 首页产品卡片图片显示不全修复
- **原因**：`.product-card-img` 高度固定 200px，产品原图均为超宽全景图（3:1~5:1 比例），`object-fit:cover` 导致上下大量裁切只剩中间一条。
- **修复**：全部 12 张产品卡片图片 `object-fit:cover` → `object-fit:contain` + `object-position:center` + `padding:8px`；容器高度从 200px 微调至 220px 给宽图更多呼吸空间。contain 模式下完整显示设备全貌，渐变背景自然填充两侧空白。
- commit 753f5ca。

## 双域名策略最终确定（用户明确：旧域展示产品 + 当邮箱域名，不展示新站）
- **实测 DNS（nslookup @8.8.8.8）**：
  - `tianyi-machinery.com` / `www` → CNAME `*.w.cdngslb.com`（**阿里云 CDN**，163.181.x / 155.102.x），**并未指向新站 pages.dev**，老站本来就独立在跑。
  - MX：`mxn.mxhichina.com`(5) / `mxw.mxhichina.com`(10) → **阿里云企业邮箱**，正常。
  - 结论：旧域 DNS **一个字都不用改**；此前担心的"两域同指一个 Pages 项目"不成立。
- **代码改动**：`_redirects` 删除 4 条旧域→新域 301 规则（与独立运营冲突，一旦旧域被加入 Pages 项目会顶掉老站），只保留 `www.tianyimachine.com → tianyimachine.com`；文件头注释写明双域名策略与禁止事项。
- 新增 `audit/dual-domain-setup.html`：双域名运营配置说明（职责划分表 / 禁止动作 / 询盘链路 / 并行 SEO 注意事项 / 待办清单）。
- commit a85cbb8。
- **红线（长期）**：① 不得把 tianyi-machinery.com 加为 Pages 自定义域；② 不得开 Cloudflare Email Routing（覆盖 mxhichina MX 会挂邮箱）；③ 不做跨域 Bulk Redirect；④ 不建议把旧域 NS 切到 Cloudflare。若 Cloudflare 里已有 pending 的旧域站点，建议 Remove Site。
- SEO 提醒已给用户：两站文案不得照搬（重复内容降权）、canonical 各指自己、不做全站页脚互链、Search Console 分别提交、NAP 信息保持一致。
