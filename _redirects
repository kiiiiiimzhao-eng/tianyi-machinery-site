# Cloudflare Pages 规范域名重定向（301）
#
# 【双域名策略】2026-08-04 确认：
#   tianyimachine.com      = 新站（本项目，Cloudflare Pages）
#   tianyi-machinery.com   = 老站，独立展示产品 + 企业邮箱域名（阿里云 CDN + 阿里云企业邮箱 MX）
#   两站互不跳转、互不绑定。因此这里【不得】添加旧域名 -> 新域名的 301 规则，
#   也不要把 tianyi-machinery.com 添加为本 Pages 项目的自定义域。
#
# 【策略变更 2026-08-04（续）】
#   实测：tianyimachine.com（apex，不带 www）在现 DNS 下无任何 A/CNAME 记录，打不开；
#   只有 www.tianyimachine.com 能正常解析到 Cloudflare。
#   因此：以 www 为规范主域（canonical / sitemap / robots 均已改为 https://www.tianyimachine.com/...）。
#   下方规则保证：一旦有人访问裸域（或将来补好 apex DNS），一律 301 收口到 www，
#   避免出现裸域死链。www 自身不做外跳，直接正常服务。
#   —— 注意：旧版这里写的是 www -> apex，那是错的（apex 打不开），已撤销。

# apex（裸域）-> www（统一收口，避免裸域死链）
https://tianyimachine.com/* https://www.tianyimachine.com/:splat 301
http://tianyimachine.com/* https://www.tianyimachine.com/:splat 301
